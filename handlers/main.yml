---
# rhel_kvm Handlers

# Reloads all kernel sysctl settings from /etc/sysctl.d/ (notified by tasks/sysctl.yml)
- name: Reload sysctl
  ansible.builtin.command:
    cmd: sysctl --system
  changed_when: false
