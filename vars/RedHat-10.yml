---
# RHEL 10 - Modular Libvirt Daemon model

# RHEL 10 packages are also signed with a post-quantum key that an older 10.1
# image doesn't have: redhat-release is updated before packages are installed
# (see tasks/packages.yml).
rhel_kvm_update_redhat_release: true

# virtqemud.service runs the VMs and brings its own sockets (BindsTo, Wants and Also in the unit file).
rhel_kvm_services:
  - virtqemud.service

# Sockets of every driver. The OS does not enable them on a fresh host (the first run reports them
# as changed), and each daemon starts on demand when its socket is used.
rhel_kvm_sockets:
  - virtqemud.socket
  - virtqemud-ro.socket
  - virtqemud-admin.socket
  - virtnetworkd.socket
  - virtnetworkd-ro.socket
  - virtnetworkd-admin.socket
  - virtstoraged.socket
  - virtstoraged-ro.socket
  - virtstoraged-admin.socket
  - virtnodedevd.socket
  - virtnodedevd-ro.socket
  - virtnodedevd-admin.socket
  - virtsecretd.socket
  - virtsecretd-ro.socket
  - virtsecretd-admin.socket
  - virtnwfilterd.socket
  - virtnwfilterd-ro.socket
  - virtnwfilterd-admin.socket

# Ensure legacy monolithic service does not conflict
rhel_kvm_disabled_services:
  - libvirtd.service
  - libvirtd.socket
  - libvirtd-ro.socket
  - libvirtd-admin.socket
